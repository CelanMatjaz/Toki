#pragma once

#include <toki/core.h>

#if defined(TK_PLATFORM_WINDOWS)

#endif

namespace toki {

// Toki application entry point (main function)
int tk_entry_point(int argc, char** args);

}  // namespace toki
